# Upload Manifest

This ZIP is the recommended clean React/Vite upload version.

Production code is under `src/`.
Original HTML references are under `reference/` and are not loaded by the app.
Hostinger visual-editor infrastructure is intentionally excluded from production runtime.

Firebase configuration and Google OAuth client ID were carried forward from the previously supplied IRPA source versions.
