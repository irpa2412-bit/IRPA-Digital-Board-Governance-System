import React,{useEffect,useMemo,useState}from"react";
import{getRecords,COLLECTIONS}from"../firebase/data";
const FILTERS={outcome:["All","For","Against","Abstain"],result:["All","Passed","Rejected","Pending"],status:["All","Completed","Locked"]};
function formatDate(v){if(!v)return"—";try{const d=v?.toDate?v.toDate():new Date(v);return Number.isNaN(d.getTime())?"—":d.toLocaleString(undefined,{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"})}catch{return"—"}}
export default function Voting(){const[rows,setRows]=useState([]),[search,setSearch]=useState(""),[outcome,setOutcome]=useState("All"),[result,setResult]=useState("All"),[status,setStatus]=useState("All"),[year,setYear]=useState("All"),[view,setView]=useState(null),[error,setError]=useState("");async function load(){try{setError("");setRows(await getRecords(COLLECTIONS.votes))}catch(e){setError(e.message||"Unable to load voting records.")}}useEffect(()=>{load()},[]);const years=useMemo(()=>["All",...new Set(rows.map(r=>{const d=r.votedAt?.toDate?r.votedAt.toDate():new Date(r.votedAt||r.createdAt);return Number.isNaN(d.getTime())?null:d.getFullYear()}).filter(Boolean).sort((a,b)=>b-a))],[rows]);const filtered=useMemo(()=>rows.filter(r=>{const q=search.trim().toLowerCase();const blob=[r.votingReference,r.meetingReference,r.resolutionReference,r.result,r.outcome,r.status].join(" ").toLowerCase();const d=r.votedAt?.toDate?r.votedAt.toDate():new Date(r.votedAt||r.createdAt);return(!q||blob.includes(q))&&(outcome==="All"||r.outcome===outcome)&&(result==="All"||r.result===result)&&(status==="All"||r.status===status)&&(year==="All"||d.getFullYear()===Number(year))}),[rows,search,outcome,result,status,year]);const metrics=useMemo(()=>({total:rows.length,for:rows.filter(x=>x.outcome==="For").length,against:rows.filter(x=>x.outcome==="Against").length,abstain:rows.filter(x=>x.outcome==="Abstain").length}),[rows]);return <div className="page"><section className="welcome-panel"><div><span className="eyebrow">IRPA GOVERNANCE EVIDENCE</span><h1>Voting Records</h1><p>Permanent historical register of votes automatically generated from IRPA meeting voting processes. Completed and locked ballots are immutable.</p></div></section>{error&&<div className="error-message action-feedback">{error}</div>}<section className="dashboard-grid"><div className="stat-card"><span>Total Votes Cast</span><strong>{metrics.total}</strong><small>Historical register</small></div><div className="stat-card"><span>For</span><strong>{metrics.for}</strong><small>Recorded ballots</small></div><div className="stat-card"><span>Against</span><strong>{metrics.against}</strong><small>Recorded ballots</small></div><div className="stat-card"><span>Abstain</span><strong>{metrics.abstain}</strong><small>Recorded ballots</small></div></section><section className="panel"><div className="panel-header"><div><h2>Historical Voting Register</h2><span>{filtered.length} of {rows.length} voting record(s)</span></div></div><div className="form-grid"><div className="form-field form-field-wide"><label>Search Voting Origin / Issue</label><input value={search}onChange={e=>setSearch(e.target.value)}placeholder="Search the issue that required voting action..."/></div><div className="form-field"><label>Vote Outcome</label><select value={outcome}onChange={e=>setOutcome(e.target.value)}>{FILTERS.outcome.map(x=><option key={x}>{x}</option>)}</select></div><div className="form-field"><label>Result</label><select value={result}onChange={e=>setResult(e.target.value)}>{FILTERS.result.map(x=><option key={x}>{x}</option>)}</select></div><div className="form-field"><label>Record Status</label><select value={status}onChange={e=>setStatus(e.target.value)}>{FILTERS.status.map(x=><option key={x}>{x}</option>)}</select></div><div className="form-field"><label>Year</label><select value={year}onChange={e=>setYear(e.target.value)}>{years.map(x=><option key={x}>{x}</option>)}</select></div></div><div className="table-wrapper"><table><thead><tr><th>Voting Origin / Issue</th><th>Meeting</th><th>Resolution</th><th>Vote</th><th>Result</th><th>Voted</th><th>Record</th></tr></thead><tbody>{filtered.length===0?<tr><td colSpan="7">No voting records match the current filters.</td></tr>:filtered.map(r=><tr key={r.id}><td><strong>{r.votingReference||"—"}</strong><div className="table-subtext">Record: {r.voteReference||r.id}</div></td><td>{r.meetingReference||r.meetingId||"—"}</td><td>{r.resolutionReference||r.resolutionId||"—"}</td><td><span className="status-badge">{r.outcome||"—"}</span></td><td>{r.result||"Pending"}</td><td>{formatDate(r.votedAt||r.createdAt)}</td><td><button type="button"onClick={()=>setView(r)}>View</button></td></tr>)}</tbody></table></div></section>{view&&<section className="panel"><div className="panel-header"><div><span className="eyebrow">IMMUTABLE VOTING RECORD</span><h2>{view.votingReference||"Voting Record"}</h2><p className="panel-description">Voter identity is intentionally not stored in the historical ballot record.</p></div><button className="secondary-button"onClick={()=>setView(null)}>Close</button></div><div className="form-grid"><div className="form-field"><label>Vote Record</label><input value={view.voteReference||view.id}readOnly/></div><div className="form-field"><label>Meeting</label><input value={view.meetingReference||view.meetingId||"—"}readOnly/></div><div className="form-field"><label>Resolution</label><input value={view.resolutionReference||view.resolutionId||"—"}readOnly/></div><div className="form-field"><label>Vote</label><input value={view.outcome||"—"}readOnly/></div><div className="form-field"><label>Result</label><input value={view.result||"Pending"}readOnly/></div><div className="form-field"><label>Status</label><input value={view.status||"Locked"}readOnly/></div><div className="form-field form-field-wide"><label>Voting Origin — Issue Requiring Voting Action</label><textarea value={view.votingReference||"—"}readOnly rows="4"/></div></div></section>}</div>}
