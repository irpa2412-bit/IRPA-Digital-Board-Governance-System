import { collection, doc, getDoc, getDocs, query, serverTimestamp, setDoc, updateDoc, where } from "firebase/firestore";
import { getDownloadURL, ref, uploadBytes } from "firebase/storage";
import { PDFDocument, StandardFonts, rgb } from "pdf-lib";
import { auth, db, storage } from "./config";
const PROFILE_COLLECTION="signatureProfiles";const ENVELOPE_COLLECTION="signatureEnvelopes";const EVENT_COLLECTION="signatureEvents";
function user(){if(!auth.currentUser)throw new Error("Authentication is required.");return auth.currentUser;}
async function hashBytes(bytes){const d=await crypto.subtle.digest("SHA-256",bytes);return Array.from(new Uint8Array(d)).map(b=>b.toString(16).padStart(2,"0")).join("");}
async function hash(file){return hashBytes(await file.arrayBuffer());}
async function uploadAsset(uid,type,file){if(!file)throw new Error(`${type} is required.`);if(!["image/png","image/jpeg","image/jpg","image/webp"].includes(file.type))throw new Error(`${type} must be PNG, JPG or WEBP.`);if(file.size>1024*1024)throw new Error(`${type} must not exceed 1 MB.`);const sha=await hash(file);const path=`signatureProfiles/${uid}/${type.toLowerCase()}-${sha}`;const r=ref(storage,path);await uploadBytes(r,file,{contentType:file.type,customMetadata:{ownerUid:uid,assetType:type,sha256:sha}});return{path,url:await getDownloadURL(r),sha};}
export async function getMySignatureProfile(){const u=user(),s=await getDoc(doc(db,PROFILE_COLLECTION,u.uid));return s.exists()?{id:s.id,...s.data()}:null;}
export async function saveMySignatureProfile({signatureFile,initialsFile,displayName,initials,method="Upload"}){const u=user();if(!String(displayName||u.displayName||"").trim())throw new Error("Display name is required.");const signature=await uploadAsset(u.uid,"Signature",signatureFile);const initialsAsset=initialsFile?await uploadAsset(u.uid,"Initials",initialsFile):null;const p={uid:u.uid,email:u.email||"",displayName:String(displayName).trim(),initials:String(initials||"").trim(),method,signaturePath:signature.path,signatureUrl:signature.url,signatureSha256:signature.sha,initialsPath:initialsAsset?.path||null,initialsUrl:initialsAsset?.url||null,initialsSha256:initialsAsset?.sha||null,status:"Active",adoptionDate:serverTimestamp(),updatedAt:serverTimestamp()};await setDoc(doc(db,PROFILE_COLLECTION,u.uid),p,{merge:true});return p;}
export async function getSignatureEnvelopes(){const u=user();const q=query(collection(db,ENVELOPE_COLLECTION),where("participantUids","array-contains",u.uid));const s=await getDocs(q);return s.docs.map(x=>({id:x.id,...x.data()})).sort((a,b)=>Number(b.createdAt?.seconds||0)-Number(a.createdAt?.seconds||0));}
export async function createSignatureEnvelope({title,documentId,documentReference,documentUrl,signingMode,recipients,fields=[]}){const u=user();if(!title?.trim())throw new Error("Envelope title is required.");if(!documentId)throw new Error("An authorized document must be selected.");if(!documentUrl)throw new Error("The selected document has no PDF file URL. Upload/link the controlled PDF before sending for signature.");if(!recipients?.length)throw new Error("At least one signer is required.");const envelopeId=`IRPA-ENV-${new Date().getFullYear()}-${Date.now().toString().slice(-8)}`;const participantUids=[u.uid,...recipients.map(r=>r.uid).filter(Boolean)];const payload={envelopeReference:envelopeId,title:title.trim(),documentId,documentReference:documentReference||"",documentUrl,senderUid:u.uid,senderEmail:u.email||"",status:"Draft",signingMode:signingMode||"Sequential",recipients,fields,participantUids,currentSignerUid:signingMode==="Sequential"?recipients[0]?.uid:null,createdAt:serverTimestamp(),updatedAt:serverTimestamp()};await setDoc(doc(db,ENVELOPE_COLLECTION,envelopeId),payload);await recordEnvelopeEvent(envelopeId,"Envelope Created",{status:"Draft"});return{id:envelopeId,...payload};}
export async function recordEnvelopeEvent(envelopeId,event,details={}){const u=user();await setDoc(doc(collection(db,EVENT_COLLECTION)),{envelopeId,event,details,actorUid:u.uid,actorEmail:u.email||"",createdAt:serverTimestamp()});}
async function fetchBytes(url){const r=await fetch(url);if(!r.ok)throw new Error(`Unable to retrieve the controlled PDF (${r.status}).`);return new Uint8Array(await r.arrayBuffer());}
async function embedImage(pdfDoc,url){const bytes=await fetchBytes(url);const lower=url.toLowerCase();if(lower.includes(".jpg")||lower.includes(".jpeg"))return pdfDoc.embedJpg(bytes);return pdfDoc.embedPng(bytes);}
function drawField(page,font,field,value){const{width,height}=page.getSize();const x=(Number(field.x||10)/100)*width;const y=height-((Number(field.y||80)/100)*height)-(Number(field.height||8)/100)*height;const w=(Number(field.width||22)/100)*width;const h=(Number(field.height||8)/100)*height;const size=Math.max(8,Math.min(18,h*.65));if(field.type==="Text"||field.type==="Name"||field.type==="Date"){page.drawText(String(value||""),{x,y:y+h*.2,size,font,color:rgb(0.05,0.05,0.08),maxWidth:w});}else if(field.type==="Checkbox"){page.drawRectangle({x,y,width:Math.min(w,h),height:Math.min(w,h),borderWidth:1,borderColor:rgb(0.1,0.1,0.1)});page.drawText("X",{x:x+2,y:y+1,size,font,color:rgb(0,0,0)});}}
export async function signEnvelope(envelope,profile,fieldValues={}){const u=user();if(!profile?.signatureUrl)throw new Error("Your Signature Profile is not configured.");if(!envelope?.documentUrl)throw new Error("This envelope has no controlled PDF URL.");if(!envelope.participantUids?.includes(u.uid))throw new Error("You are not a participant in this signing envelope.");if(envelope.signingMode==="Sequential"&&envelope.currentSignerUid&&envelope.currentSignerUid!==u.uid)throw new Error("This envelope is waiting for another signer.");const source=await fetchBytes(envelope.signedDocumentUrl||envelope.documentUrl);const originalHash=await hashBytes(source);const pdf=await PDFDocument.load(source);const font=await pdf.embedFont(StandardFonts.Helvetica);const signature=await embedImage(pdf,profile.signatureUrl);const initials=profile.initialsUrl?await embedImage(pdf,profile.initialsUrl):null;const signerFields=(envelope.fields||[]).filter(f=>f.signerUid===u.uid);for(const field of signerFields){const page=pdf.getPages()[Math.max(0,Number(field.page||1)-1)];if(!page)continue;const{width,height}=page.getSize();const x=(Number(field.x||10)/100)*width;const y=height-((Number(field.y||80)/100)*height)-(Number(field.height||8)/100)*height;const w=(Number(field.width||24)/100)*width;const h=(Number(field.height||9)/100)*height;if(field.type==="Signature"){page.drawImage(signature,{x,y,width:w,height:h});}else if(field.type==="Initials"&&initials){page.drawImage(initials,{x,y,width:w,height:h});}else{const value=fieldValues[field.fieldId]??(field.type==="Name"?profile.displayName:field.type==="Date"?new Date().toLocaleDateString():fieldValues[field.fieldId]||"");drawField(page,font,field,value);}}const signedBytes=await pdf.save();const finalHash=await hashBytes(signedBytes);const path=`signatureEnvelopes/${envelope.id}/signed-${u.uid}-${finalHash}.pdf`;const storageRef=ref(storage,path);await uploadBytes(storageRef,signedBytes,{contentType:"application/pdf",customMetadata:{envelopeId:envelope.id,signerUid:u.uid,originalHash,finalHash}});const signedUrl=await getDownloadURL(storageRef);const nextIndex=(envelope.recipients||[]).findIndex(r=>r.uid===u.uid)+1;const nextRecipient=envelope.recipients?.[nextIndex];const completed=!nextRecipient||envelope.signingMode==="Parallel";const update={status:completed?"Completed":"In Progress",lastSignedByUid:u.uid,lastSignedAt:serverTimestamp(),signedDocumentPath:path,signedDocumentUrl:signedUrl,originalDocumentHash:originalHash,latestDocumentHash:finalHash,updatedAt:serverTimestamp()};if(!completed)update.currentSignerUid=nextRecipient.uid;if(completed)update.completedAt=serverTimestamp();await updateDoc(doc(db,ENVELOPE_COLLECTION,envelope.id),update);await setDoc(doc(collection(db,"signatures")),{envelopeId:envelope.id,documentId:envelope.documentId,documentReference:envelope.documentReference,signerUid:u.uid,signerEmail:u.email||"",signerName:profile.displayName,signatureMethod:profile.method||"Upload",signatureSha256:profile.signatureSha256,status:"Signed",signedAt:serverTimestamp(),originalDocumentHash:originalHash,signedDocumentHash:finalHash,immutable:true});await recordEnvelopeEvent(envelope.id,"Document Signed",{signerUid:u.uid,signerName:profile.displayName,originalDocumentHash:originalHash,signedDocumentHash:finalHash,status:completed?"Completed":"In Progress"});return{...envelope,...update,id:envelope.id};}
export async function recordSignatureAdoption({documentId,documentReference,fieldId,signatureProfile}){const u=user(),r=doc(collection(db,"signatures"));await setDoc(r,{documentId:documentId||null,documentReference:documentReference||"",fieldId:fieldId||"",signerUid:u.uid,signerEmail:u.email||"",signerName:signatureProfile?.displayName||u.displayName||"",signatureMethod:signatureProfile?.method||"Upload",signatureSha256:signatureProfile?.signatureSha256||null,status:"Signed",signedAt:serverTimestamp(),createdAt:serverTimestamp(),immutable:true});return r.id;}
export async function createCompletionCertificate(envelope){const u=user();const pdf=await PDFDocument.create();const page=pdf.addPage([595,842]);const font=await pdf.embedFont(StandardFonts.Helvetica);const bold=await pdf.embedFont(StandardFonts.HelveticaBold);page.drawText("IRPA DIGITAL GOVERNANCE SYSTEM",{x:48,y:790,size:11,font:bold,color:rgb(.15,.2,.3)});page.drawText("CERTIFICATE OF COMPLETION",{x:48,y:750,size:24,font:bold});const rows=[["Envelope",envelope.envelopeReference],["Subject",envelope.title],["Document",envelope.documentReference],["Status",envelope.status],["Completed",new Date().toISOString()],["Final document hash",envelope.latestDocumentHash||"—"]];let y=700;for(const [a,b] of rows){page.drawText(a,{x:48,y,size:10,font:bold});page.drawText(String(b),{x:190,y,size:10,font});y-=34;}page.drawText("SIGNER EVENTS",{x:48,y:y-10,size:13,font:bold});y-=40;for(const r of envelope.recipients||[]){page.drawText(`${r.name||r.email} — ${r.email||""}`,{x:48,y,size:10,font});y-=20;}page.drawText("This certificate records the signing transaction captured by the IRPA Signature Platform. It is an electronic transaction record, not a PKI certificate.",{x:48,y:80,size:8,font,maxWidth:500});const bytes=await pdf.save();const certificateHash=await hashBytes(bytes);const path=`signatureEnvelopes/${envelope.id}/certificate-${certificateHash}.pdf`;const r=ref(storage,path);await uploadBytes(r,bytes,{contentType:"application/pdf",customMetadata:{envelopeId:envelope.id,certificateHash}});const url=await getDownloadURL(r);await updateDoc(doc(db,ENVELOPE_COLLECTION,envelope.id),{certificatePath:path,certificateUrl:url,certificateHash,updatedAt:serverTimestamp()});await recordEnvelopeEvent(envelope.id,"Certificate Generated",{certificateHash,actorUid:u.uid});return url;}
