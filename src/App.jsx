import React,{useEffect,useState}from"react";
import Invitations from"./pages/Invitations";import Employees from"./pages/Employees";import EmployeePayments from"./pages/EmployeePayments";import Meetings from"./pages/Meetings";
import{observeAuthState,loginWithEmail,loginWithGoogle,registerWithEmail,sendPasswordReset,sendAdminMagicLink,isMagicLink,completeMagicLink,logout}from"./firebase/auth";
import{getAdminProfile,getCurrentMemberProfile,getCurrentEmployeeProfile,provisionCurrentMemberFromInvitation}from"./firebase/data";
const NAV={GOVERNANCE:["Dashboard","Members & Personnel","Invitations","Participants","Meetings","Meeting Room","Transcription & Proceedings","Resolutions","Voting","Actions","Documents","Signature Platform","Decisions","Risk Register","Authorization & Approvals","Employee Payments"],FINANCE:["Finance Portfolio","Procurement"],EVIDENCE:["Reports","Audit Trail"],SYSTEM:["Settings"]};
const ALL_MODULES=Object.values(NAV).flat();const BASE_MEMBER_MODULES=["Dashboard","Meetings","Meeting Room","Transcription & Proceedings","Resolutions","Voting","Actions","Documents","Signature Platform","Decisions","Authorization & Approvals","Employee Payments","Reports"];const FINANCE_ROLES=["Finance Personnel","Finance Manager","Accountant","Finance Officer","Executive Director"];const FINANCE_OVERSIGHT_ROLES=["Executive Director","Director Internal Oversight","Internal Oversight Officer"];const HR_ROLES=["Executive Director","Director Human Resources","HR Manager"];
function memberModules(p){const r=p?.role||"",m=[...BASE_MEMBER_MODULES];if(FINANCE_ROLES.includes(r))m.splice(m.indexOf("Reports"),0,"Finance Portfolio","Procurement");if(HR_ROLES.includes(r))m.splice(m.indexOf("Reports"),0,"Members & Personnel");return[...new Set(m)];}
function Logo({className=""}){return <img className={`irpa-logo ${className}`}src="/irpa-logo.jpg"alt="Improvement of Rangeland in Pastoral Areas logo"/>}
function AuthScreen(){const[mode,setMode]=useState("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setMessage("");try{if(mode==="register"){await registerWithEmail(email.trim(),password);setMessage("Account created. Please verify your email before continuing")}else await loginWithEmail(email.trim(),password)}catch(x){setMessage(x.message||"Authentication failed.")}finally{setBusy(false)}}async function reset(){if(!email.trim()){setMessage("Enter your email address first.");return}setBusy(true);try{await sendPasswordReset(email.trim());setMessage("Password reset email sent")}catch(x){setMessage(x.message||"Unable to send password reset email.")}finally{setBusy(false)}}async function adminLink(){if(!email.trim()){setMessage("Enter the authorised administrator email address.");return}setBusy(true);try{await sendAdminMagicLink(email.trim());setMessage("Administrator sign-in link sent")}catch(x){setMessage(x.message||"Unable to send administrator sign-in link.")}finally{setBusy(false)}}return <main className="auth-screen"><section className="auth-card"><div className="auth-brand"><Logo className="auth-logo"/><div className="brand-kicker">IRPA</div><h1>Digital Board Governance</h1><p>Secure institutional governance, authorization, finance and employee management for Improvement of Rangeland in Pastoral Areas.</p></div><div className="auth-divider"><span>{mode==="login"?"Secure sign in":"Create account"}</span></div><form onSubmit={submit}><label>Email address</label><input type="email"value={email}onChange={e=>setEmail(e.target.value)}required/><label>Password</label><input type="password"value={password}onChange={e=>setPassword(e.target.value)}required minLength={6}/><button type="submit"disabled={busy}>{busy?"Please wait...":mode==="register"?"Create Account":"Sign In Securely"}</button></form><button className="secondary-button"onClick={async()=>{setBusy(true);try{await loginWithGoogle()}catch(x){setMessage(x.message||"Google sign-in failed.")}finally{setBusy(false)}}}disabled={busy}>Continue with Google</button><div className="auth-links"><button className="text-button"onClick={reset}disabled={busy}>Forgot password?</button><button className="text-button"onClick={adminLink}disabled={busy}>Administrator Gateway</button><button className="text-button"onClick={()=>{setMode(mode==="login"?"register":"login");setMessage("")}}disabled={busy}>{mode==="login"?"Create a member account":"Return to sign in"}</button></div>{message&&<div className="auth-message">{message}</div>}</section></main>}
function Loading(){const[slow,setSlow]=useState(false);useEffect(()=>{const t=setTimeout(()=>setSlow(true),8000);return()=>clearTimeout(t)},[]);return <div className="loading-screen"><Logo className="loading-logo"/><div><strong>IRPA Digital Governance</strong><div className="loading-sub">{slow?"Authorization check is taking longer than expected.":"Establishing secure IRPA session..."}</div>{slow&&<button onClick={()=>window.location.reload()}>Retry Secure Session</button>}</div></div>}
function AccessDenied({user,reason}){return <main className="auth-screen"><section className="auth-card"><Logo className="auth-logo"/><h1>Access Not Authorised</h1><p>{reason||"No active IRPA authorization profile was found."}</p><p className="muted">{user?.email}</p><button onClick={logout}>Sign Out</button></section></main>}
function Dashboard({admin,employee}){const cards=admin?[["Employees","0"],["Members","0"],["Meetings","0"],["Pending Authorizations","0"],["Open Actions","0"],["Active Risks","0"]]:[["My Meetings","0"],["Actions Assigned","0"],["Pending Authorizations","0"],["My Payment Requests","0"],["Pending Signatures","0"],["Reports","0"]];return <div className="page"><section className="welcome-panel"><div><span className="eyebrow">IRPA DIGITAL GOVERNANCE</span><h1>{admin?"Governance Dashboard":"Employee & Governance Dashboard"}</h1><p>{admin?"Central workspace for IRPA Board governance, organizational oversight and controlled administration.":"Your authorized IRPA governance and employee workspace."}</p></div>{employee?.employeeNumber&&<div className="identity-card"><span>Employee Number</span><strong>{employee.employeeNumber}</strong></div>}</section><div className="dashboard-grid">{cards.map(([a,b])=><div className="stat-card"key={a}><span>{a}</span><strong>{b}</strong><small>Current register</small></div>)}</div></div>}
function Finance({profile}){return <section className="module-panel"><span className="eyebrow">FINANCE & ADMINISTRATION</span><h1>Finance Portfolio</h1><p>Restricted financial management and Board-approved budget execution workspace.</p><div className="dashboard-grid">{["Approved Budgets","Budget Execution","Transactions","Funding & Grants","Commitments","Financial Reports"].map(x=><div className="stat-card"key={x}><span>{x}</span><strong>0</strong><small>Current register</small></div>)}</div><div className="module-status"><strong>Access authority</strong><span>{profile?.role} — authorized by organizational title.</span></div></section>}
function Authorization(){return <section className="module-panel"><span className="eyebrow">CONTROLLED WORKFLOW</span><h1>Authorization & Approvals</h1><p>Controlled organizational workflow with responsible officers, reviewers, approvers, copied members and explicit action protocols.</p><div className="dashboard-grid">{["Requests Lodged","My Actions","Awaiting Review","Awaiting Authorization","Escalated to Board","Completed"].map(x=><div className="stat-card"key={x}><span>{x}</span><strong>0</strong><small>Workflow status</small></div>)}</div><div className="module-status"><strong>Authority protocol</strong><span>Employee request → responsible officer → reviewers → delegated authorization → implementation → reporting. Reserved matters escalate to the Board.</span></div></section>}
function Placeholder({name}){return <section className="module-panel"><span className="eyebrow">IRPA GOVERNANCE MODULE</span><h1>{name}</h1><p>This module is connected to the IRPA Digital Governance Workspace.</p><div className="module-status"><strong>Module foundation ready</strong><span>Functional workflow and Firebase data integration will be implemented here.</span></div></section>}
function Shell({user,profile,admin,employee}){const[active,setActive]=useState("Dashboard"),modules=admin?ALL_MODULES:memberModules(profile),financeVisible=FINANCE_ROLES.includes(profile?.role)||FINANCE_OVERSIGHT_ROLES.includes(profile?.role),peopleVisible=admin||HR_ROLES.includes(profile?.role);useEffect(()=>{if(!modules.includes(active))setActive("Dashboard")},[profile?.role,admin]);const groups=Object.entries(NAV).map(([title,items])=>[title,items.filter(x=>modules.includes(x))]).filter(([,items])=>items.length);return <div className="app-shell"><aside className="sidebar"><div className="sidebar-brand"><Logo className="sidebar-logo"/><div className="sidebar-brand-copy"><strong>IRPA</strong><span>Digital Governance</span><small>Improvement of Rangeland in Pastoral Areas</small></div></div><div className="authority-card"><span>{admin?"ADMINISTRATOR":"AUTHORIZED USER"}</span><strong>{profile?.role}</strong>{employee?.employeeNumber&&<small>{employee.employeeNumber}</small>}</div><nav>{groups.map(([title,items])=><div className="nav-group"key={title}><div className="nav-group-title">{title}</div>{items.map(x=><button key={x}className={active===x?"nav-item active":"nav-item"}onClick={()=>setActive(x)}><span>{x}</span>{active===x&&<i/>}</button>)}</div>)}</nav><div className="sidebar-footer"><div className="secure-label">● Secure IRPA workspace</div><button className="logout-button"onClick={logout}>Sign Out</button></div></aside><section className="main-area"><header className="topbar"><div><span className="topbar-kicker">IMPROVEMENT OF RANGELAND IN PASTORAL AREAS</span><h2>{active}</h2><p>Digital Board Governance Workspace</p></div><div className="user-info"><div className="user-avatar">{(profile?.name||user.email||"I").slice(0,1).toUpperCase()}</div><div><strong>{profile?.name||"IRPA User"}</strong><span>{profile?.role||user.email}</span></div></div></header><main className="content-area">{active==="Dashboard"&&<Dashboard admin={admin}employee={employee}/>} {active==="Invitations"&&admin&&<Invitations/>} {active==="Members & Personnel"&&peopleVisible&&<Employees/>} {active==="Meetings"&&<Meetings/>} {active==="Employee Payments"&&<EmployeePayments profile={{...profile,...employee}}/>} {active==="Finance Portfolio"&&financeVisible&&<Finance profile={profile}/>} {active==="Authorization & Approvals"&&<Authorization/>} {!(["Dashboard","Invitations","Members & Personnel","Meetings","Employee Payments","Finance Portfolio","Authorization & Approvals"].includes(active))&&<Placeholder name={active}/>}</main></section></div>}
export default function App(){const[user,setUser]=useState(undefined),[profile,setProfile]=useState(undefined),[employee,setEmployee]=useState(null),[error,setError]=useState("");useEffect(()=>observeAuthState(async u=>{setUser(u);setProfile(undefined);setEmployee(null);setError("");if(!u){setProfile(null);return}try{const a=await getAdminProfile(u.uid);if(a?.active===true){setProfile({...a,authorizationType:"administrator"});return}let m=await getCurrentMemberProfile();const invitationId=new URLSearchParams(window.location.search).get("memberInvite");if(!m&&invitationId){await provisionCurrentMemberFromInvitation(invitationId);m=await getCurrentMemberProfile();if(m)window.history.replaceState({},document.title,window.location.pathname)}if(!m){setError("This account has no active IRPA member profile.");setProfile(null);return}if(m.status!=="Active"){setError("The IRPA member profile exists but is not active.");setProfile(null);return}setProfile({...m,authorizationType:"member"});setEmployee(await getCurrentEmployeeProfile())}catch(x){console.error(x);setError(x.message||"Unable to verify IRPA authorization.");setProfile(null)}}),[]);useEffect(()=>{async function magic(){if(!isMagicLink())return;const e=window.localStorage.getItem("irpaEmailForSignIn");if(!e)return;try{await completeMagicLink(e);window.localStorage.removeItem("irpaEmailForSignIn")}catch(x){console.error(x)}}magic()},[]);if(user===undefined||profile===undefined)return <Loading/>;if(!user)return <AuthScreen/>;if(!profile)return <AccessDenied user={user}reason={error}/>;return <Shell user={user}profile={profile}employee={employee}admin={profile.authorizationType==="administrator"}/>}
