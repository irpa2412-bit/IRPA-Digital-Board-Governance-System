rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // BUILD PHASE ONLY: authenticated users can exercise the interdependent application.
    // Replace with architecture-specific rules after functional testing is complete.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
